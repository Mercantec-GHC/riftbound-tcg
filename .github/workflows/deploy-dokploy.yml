name: Deploy frontend to Dokploy

on:
  push:
    branches:
      - main
    paths:
      - frontend/**

permissions: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Dokploy deployment
        run: |
          curl -X POST "https://example.com/replace-with-your-dokploy-curl-command"
